import React from 'react';
import { Button } from '@/components/ui/button';
import { publishIcon } from '@/definitions/constants';

const PublishFormBtn = () => {
  return <Button>{publishIcon} Publish</Button>;
};

export default PublishFormBtn;
